apiVersion: bastion.io/v1alpha1
kind: BackupPolicy
metadata:
  labels:
    app.kubernetes.io/name: bastion
    app.kubernetes.io/managed-by: kustomize
  name: backuppolicy-sample
spec:
  # TODO(user): Add fields here
