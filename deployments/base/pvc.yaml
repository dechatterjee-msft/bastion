apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bastion-backup-pvc
  namespace: bastion-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
